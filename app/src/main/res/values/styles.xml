<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Style untuk judul toolbar -->
    <style name="ToolbarTitleText" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style untuk tombol dengan gaya outline -->
    <style name="ElegantOutlinedButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/colorPrimaryLight</item>
        <item name="strokeColor">@color/colorPrimaryLight</item>
        <item name="iconTint">@color/colorPrimaryLight</item>
        <item name="android:padding">12dp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <!-- Style untuk tombol dengan gaya filled -->
    <style name="ElegantFilledButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/colorOnPrimaryLight</item>
        <item name="backgroundTint">@color/colorPrimaryLight</item>
        <item name="android:padding">12dp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <!-- Style untuk input text yang elegan -->
    <style name="ElegantEditText" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/colorPrimaryLight</item>
        <item name="hintTextColor">@color/colorPrimaryLight</item>
    </style>
</resources>

